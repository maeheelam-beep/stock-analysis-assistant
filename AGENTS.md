# AGENTS.md

本文件适用于当前目录及其全部子目录。后续在本仓库工作的 Codex 或其他编码代理，必须先阅读本文件，再修改代码。

## 1. 项目目标

- 产品名称：股基罗盘。
- 当前基线：V1.3.32，私人试用版，约 10 人规模。
- 核心范围：A 股、场内 ETF、场外公募基金的持仓、行情、资讯和 AI 分析。
- 美股只用于夜间观察，不做美股持仓分析、综合评分或 AI 操作建议。
- 美股自选按匿名设备持久化保存，搜索结果点选后写入，支持删除；不得把美股自选并入持仓或 DeepSeek 分析输入。
- 当前不做港股、自动交易、公开注册、收费、广告或商业推广。
- 网站内容只供个人研究参考，不构成投资建议或交易指令。

## 2. 权威资料

功能变更前后应核对并同步以下文件：

- 产品基线：`../../01-输入资料/股基罗盘 V1.md`
- 项目说明：`../../README.md`
- 变更日志：`../../00-项目管理/变更日志.md`
- 任务清单：`../../00-项目管理/任务清单.md`
- 源码说明：`README.md`

产品功能发生实质变化时，应更新版本记录和上述相关文档；纯重构、注释或测试修复无需强行提升产品版本。

## 3. 技术栈与目录

- Node.js：`>=22.13.0`
- 框架：vinext、React 19、TypeScript
- 部署结构：Cloudflare Worker 兼容 ESM，保留 `.openai/hosting.json`
- 数据库：D1/SQLite、Drizzle ORM
- 页面与交互：`app/page.tsx`
- 全局样式：`app/globals.css`
- 设计令牌：`tokens.css`
- API：`app/api/`
- 数据访问与外部行情：`lib/`
- 数据库定义：`db/`、`drizzle/`
- 回归测试：`tests/*.test.mjs`

不得为了方便而替换现有包管理器、锁文件、vinext 架构或 Cloudflare 配置。

## 4. 数据真实性

- 禁止虚构行情、涨跌、评分、资讯、K 线、历史统计或 AI 结论。
- 数据缺失时使用 `null`、空状态或明确错误，不得填入看似真实的演示数字。
- 行情必须保留来源、时间、状态和统计范围。
- A 股/ETF 持仓行情、证券搜索、股票复权日线和个股估值优先并行请求两个免费公开源；成功双源必须返回来源列表、来源数和核验状态。行情及最新共同收盘偏差超过 0.5% 标记来源冲突，估值同字段偏差超过 10% 标记来源冲突；不得静默平均或掩盖差异。仅一源可用时允许降级，但页面必须明确标记单源。
- A 股涨幅榜、跌幅榜和成交额榜必须来自沪深京实时排行，每类最多显示 20 条；不得使用固定股票池补位。
- A 股股票三榜以新浪财经实时排行作为主源、东方财富作为自动备用；市场宽度优先按 500 只成交活跃的动态沪深京股票样本计算，失败时按三榜去重样本降级，并明确披露实际样本量和口径，不得把样本统计冒充全市场家数。股票三榜继续独立于市场宽度，宽度异常不得清空排行。
- A 股与美股的股票行情榜至少统一展示名称/代码、最新价、涨跌额和涨跌幅；字段缺失时显示空值，不得反推或虚构。
- 股票持仓表固定按“股票、今盈亏、总盈亏、总收益率、现价、成本、数量、市值/仓位、行情”展示；今盈亏按 `(现价 - 昨收) × 数量` 计算，总盈亏按 `(现价 - 成本) × 数量` 计算，缺失项留空。
- 首页、股票页和基金页必须分别展示对应范围的总资产、今日盈亏、累计盈亏与累计收益率；总资产仅统计网站已录入持仓，不含未录入现金。场外基金没有可靠日内净值时不计入今日盈亏，并明确提示待净值结算。
- 基金经理档案优先由天天基金与新浪基金双源核对；重仓重叠只使用公开定期报告。经理任职回报必须标注为本轮任职口径。重仓重叠至少需要两只基金取得披露，仅比较各基金最新报告的前十大股票，报告期与合计权重必须明确展示，不得表述为完整实时持仓。
- 基金历史相似只使用该基金自身公开单位净值序列，固定比较最近 15 个开放日的日收益形态，最多回看 780 个开放日；相似样本少于 3 个时不得输出统计结论，页面必须披露净值截止日、样本数、后续区间收益、最大回撤和“历史相似不代表未来表现”。
- A 股行业板块必须来自实时行业排行；新浪财经行业表为主源、东方财富行业排行为自动备用，主源异常时才调用备用；行业热力图固定取涨幅前 24 与跌幅前 24，目标 48 个动态样本；使用无空洞矩形树图完整填满画布，面积单调参考成交额并压缩极端差距，红涨绿跌且颜色深浅表示涨跌幅强度；行业样本单独缓存 60 秒，失败时可沿用 30 分钟内最后一次成功结果，仍不可用时明确留空，不得使用固定行业 ETF 补位。
- 图形只能由真实数据计算；没有时序数据时，不得伪造趋势折线。
- 市场资讯必须采用独立多源容错：一个来源失败不得清空其他来源；同一事件可合并，但必须保留各来源名称与原文链接。媒体内容只聚合标题、简短摘要和原文入口，不复制全文；证监会、中国政府网等官方消息单列为政策，不得与持仓公告混淆。
- 国内市场统一使用红涨、绿跌。
- A 股和美股行情在各自常规交易时段每 30 秒自动更新，其他时段每 5 分钟；页面隐藏时暂停、恢复可见后立即同步。资讯与公告每 5 分钟更新；手动刷新至少间隔 30 秒。服务端行情上游结果缓存 15 秒并合并同一缓存键的并发请求。节假日暂按工作日时间窗口判断。

## 5. DeepSeek、隐私与密钥

- 只使用 DeepSeek 官方 API；密钥仅允许存在于服务端环境变量 `DEEPSEEK_API_KEY`。
- 禁止把真实密钥写入源码、Markdown、测试、提交记录、日志或前端响应。
- 如果对话中出现真实密钥，不得在回复、命令输出或文件中再次完整展示。
- 每次 AI 分析都必须由用户单独确认，不得后台静默调用。
- 发给 DeepSeek 的持仓内容只能是匿名编号、持仓类型、组合占比、四舍五入后的涨跌或收益百分比、匿名化估值/财报与基金档案摘要、行业强弱、匿名集中度与逐只风险、脱敏资讯标题和历史统计；可同时发送不含用户身份的公开指数、市场宽度、行业排行和市场快讯/政策摘要。
- 不得向 DeepSeek 发送证券代码、证券名称、平均成本、持有数量、设备标识或 Cookie。
- 搜索源只接收用户当次输入的关键词；行情、基金和公告源只接收必要的证券代码。
- 关键行情缺失或过期时，应停止输出 AI 参考动作并说明原因。
- AI 可以给出鲜明倾向、明天/下一交易日的条件式预测和参考动作，但必须把事实、计算与推断分开。明日输出需要包含偏涨/震荡/偏跌/看不清、判断把握、合计为 100 的三种主观概率、最需关注的具体持仓、开盘观察点、参考动作与动作条件；逐只动作必须绑定一个匿名持仓别名，服务端还原后页面固定显示“真实标的名称 + 动作 + 原因 + 什么时候做”，不得只给无法区分对象的组合级笼统动作。用普通用户能直接理解的短句，避免堆砌投研术语。不得把概率写成保证，不得把历史相似表述为确定未来。

## 6. 产品与设计约束

- 视觉方向是简洁、专业、克制的“私人投研桌面”，避免通用 SaaS 卡片堆叠和花哨动画。
- 复用 `tokens.css` 的语义令牌；不要在组件中散落新的硬编码颜色、字体和阴影。
- 首页策略罗盘是主要签名视觉；其他页面以信息层级、表格和留白为主。
- 主导航顺序固定为：首页、行情、股票、基金、资讯、美股；可见标签保持两个汉字。
- A 股行情保持“指数概览 + 统一市场中心 + 主排行表 + 内嵌搜索”的结构。
- 所有功能必须兼顾桌面端和手机端，局部数据表可以横向滚动，但页面根节点不得横向溢出。
- 交互控件必须有键盘焦点、可识别标签和足够触控尺寸；尊重减少动态效果设置。
- 对标网站只能用于提取信息架构和交互规律，不复制其品牌、广告、图片或独特视觉素材。

## 7. 修改原则

- 先阅读与任务直接相关的文件，再做最小、完整、可验证的修改。
- 保留用户已有改动；不要覆盖或回退与当前任务无关的工作树变化。
- 未经明确授权，不删除数据、迁移、配置、持仓记录或项目目录。
- 不使用 `git reset --hard`、`git checkout --` 等破坏性命令。
- 新能力应接入现有组件、类型和 API，不创建重复的数据链路或平行设计系统。
- API 响应和前端类型必须同步修改；数据库结构变化必须生成并检查 Drizzle 迁移。
- 不以“后续再补”为理由留下假按钮、空链接或没有实现的可点击标签。

## 8. 验证要求

代码修改完成后，至少运行：

```bash
npm run lint
npm run build
node --test
```

按改动范围补充验证：

- 行情、搜索、资讯或 AI 接口变化：执行对应 API 冒烟测试。
- 数据库结构变化：运行 `npm run db:generate`，检查迁移内容，再运行构建和测试。
- 页面测试断言应验证真实产品文案和关键结构，并继续阻止演示数据回归。
- 不得通过删除失败测试、降低断言或硬编码返回值来让验证通过。

## 9. 部署边界

- 当前默认只在本地运行，不自动部署、不购买服务器或域名。
- 只有用户明确要求部署时，才进入托管流程；部署前再次确认 Secret、数据库迁移、风险声明、隐私说明和数据源说明。
- `.env.local` 不得提交；新增环境变量时同步更新不含真实值的 `.env.example`。

## 10. 交付说明

完成任务时应简要说明：

- 用户可见的结果；
- 修改过的关键文件；
- 已执行的验证及结果；
- 免费数据源、隐私、部署或未完成功能方面仍存在的限制。

不要把未实现、未验证或仅计划中的能力描述为已完成。
